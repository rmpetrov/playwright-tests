[pytest]
pythonpath = .
addopts = --strict-markers -ra

markers =
    ui: UI tests (Playwright browser tests)
    api: API tests (HTTP requests)
    integration: Integration tests (real HTTP, may be slow or flaky)
    flaky: Temporarily unstable test allowed to retry in CI with issue tracking
    quarantine: Non-blocking test excluded from gating pipeline until fixed
